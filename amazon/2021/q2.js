/*

Given a string consisting of stars and pipes, and two arrays holding the starting and the ending indices.
For each [startIndice - endIndice] pairs, return the number of starts wrapped by pipes.

Ex.

s = *|**|*|*
starts = [1, 1]
ends = [5, 6]

result = [2, 3]

*/
